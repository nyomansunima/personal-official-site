import { FunctionComponent } from 'react'

/**
 * # BlogFooter
 *
 * Footer for the blog page
 * that showing acros the blog and blog detail
 *
 * @returns JSX.Element
 */
const BlogFooter: FunctionComponent = (): JSX.Element => {
  return <footer></footer>
}

export default BlogFooter
